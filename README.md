# Togalma CLI

Developer-friendly CLI to authenticate, browse the menu, create an order, and pay with Wave.

## Install

```bash
npm i -g @togalma/cli
```

## Usage

```bash
togalma auth login
togalma menu
togalma order create
togalma order pay <recOrderId>
togalma orders list
```